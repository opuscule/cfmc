---
titleTag: Blog
pageName: blog
description: to complete
layout: 'base.njk'
---


<section class="container my-20 my-md-12">
	<div class="row">
		
		<div class="col text-center">
			<h1 class="display-2 mb-20">Blog</h1>
		</div>
	</div>
	{%- for post in collections.post | reverse -%}
	<div class="row each-post justify-content-center align-items-center mb-18">
		<div class="col-md-5">
			<h2 class="display-5 mb-4"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
			<p class="ep-meta">by {{ post.data.author }} on {{ post.date | postDate }}</p>
			<p>{{ post.data.description }}&hellip;</p>
		</div>
		<div class="col-md-3">
			<a href="{{ post.url }}"><img src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}" /></a>
		</div>
	</div>
	{%- endfor -%}
		
</section>